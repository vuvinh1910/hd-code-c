#include <stdio.h>
#include <string.h>
#include <stdio.h>
int main() {
    char str1[] = "Hello";
    char str2[] = "World";
    int sosanh = strcmp(str1, str2);
    // sosanh = 0 nếu 2 mảng giống nhau
    // >0 nếu mảng 1 lớn hơn mảng 2 và ngược lại
    return 0;
}
